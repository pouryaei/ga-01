name: 1 - hello - world

on: push

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print hello
              run: echo "Hello world"
    second-job:
        runs-on: ubuntu-latest
        steps:
            - name: Print Goodbye
              run: echo "Say hi to you"


            - name: i will fail
              run: exit 1

            - name: say Goodbye
              run: echo "Goodbye"
              
